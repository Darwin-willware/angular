import { ConditionaldirectDirective } from './conditionaldirect.directive';

describe('ConditionaldirectDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionaldirectDirective();
    expect(directive).toBeTruthy();
  });
});
