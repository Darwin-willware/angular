import { HostbindDirective } from './hostbind.directive';

describe('HostbindDirective', () => {
  it('should create an instance', () => {
    const directive = new HostbindDirective();
    expect(directive).toBeTruthy();
  });
});
