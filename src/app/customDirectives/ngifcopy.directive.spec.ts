import { NgifcopyDirective } from './ngifcopy.directive';

describe('NgifcopyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgifcopyDirective();
    expect(directive).toBeTruthy();
  });
});
